goal(goal_1, if, stateOf(occupied, location)).
goal(goal_1, then, stateOf(lit, location)).


% goal(@id_hash("illuminate the room when it is occupied"), if, stateOf(occupied, location), "illuminate the room when it is occupied").
% goal(@id_hash("illuminate the room when it is occupied"), then, stateOf(lit, location), "illuminate the room when it is occupied").
